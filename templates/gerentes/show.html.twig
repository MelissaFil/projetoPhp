{% extends 'base.html.twig' %}

{% block title %}Gerentes{% endblock %}

{% block body %}
    <h1>Gerentes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gerente.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ gerente.nome }}</td>
            </tr>
            <tr>
                <th>Cpf</th>
                <td>{{ gerente.cpf }}</td>
            </tr>
            <tr>
                <th>Telefone</th>
                <td>{{ gerente.telefone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gerentes_index') }}">back to list</a>

    <a href="{{ path('app_gerentes_edit', {'id': gerente.id}) }}">edit</a>

    {{ include('gerentes/_delete_form.html.twig') }}
{% endblock %}
